## Python Backend Setup

This project uses a single Python virtual environment: `.venv`.

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

Run the neural network script:

```bash
source .venv/bin/activate
python api/NN.py
```
